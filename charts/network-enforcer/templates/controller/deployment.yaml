apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "network-enforcer.fullname" . }}-controller-manager
  labels:
  {{- include "network-enforcer.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.controller.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/component: controller
      {{- include "network-enforcer.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: controller
        {{- include "network-enforcer.labels" . | nindent 8 }}
        {{- with .Values.controller.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        kubectl.kubernetes.io/default-container: manager
      {{- with .Values.controller.podAnnotations }}
      {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
      {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
      - args:
        {{- toYaml .Values.controller.args | nindent 8 }}
        - --wnp-status-reconciler-update-interval={{ .Values.controller.wnpStatusUpdateInterval }}
        command:
        - /controller
        env:
        - name: KUBERNETES_CLUSTER_DOMAIN
          value: {{ quote .Values.kubernetesClusterDomain }}
        {{- include "network-enforcer.otel.config.env" . | indent 8 }}
        {{- with .Values.controller.env }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        image: {{ .Values.controller.image.repository }}:{{ .Values.controller.image.tag
          | default .Chart.AppVersion }}
        imagePullPolicy: {{ .Values.controller.image.pullPolicy }}
        name: manager
        ports:
        - containerPort: 4317
          name: otlp-grpc
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources: {{- toYaml .Values.controller.resources | nindent 10 }}
        securityContext: {{- toYaml .Values.controller.containerSecurityContext
          | nindent 10 }}
        volumeMounts:
        {{- if .Values.controller.webhook.enabled }}
        - name: webhook-certs
          mountPath: /tmp/k8s-webhook-server/serving-certs
          readOnly: true
        {{- end }}
        {{- include "network-enforcer.otel.config.volumeMounts" . | indent 8 }}
      volumes:
      {{- if .Values.controller.webhook.enabled }}
      - name: webhook-certs
        csi:
          driver: "csi.cert-manager.io"
          readOnly: true
          volumeAttributes:
            csi.cert-manager.io/issuer-name: {{ include "network-enforcer.caIssuerName" . }}
            csi.cert-manager.io/issuer-kind: Issuer
            csi.cert-manager.io/dns-names: {{ include "network-enforcer.fullname" . }}-controller-webhook.{{ .Release.Namespace }}.svc
      {{- end }}
      {{- include "network-enforcer.otel.config.volumes" . | indent 6 }}
      securityContext: {{- toYaml .Values.controller.podSecurityContext | nindent
        8 }}
      {{- with .Values.controller.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.controller.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.controller.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "network-enforcer.fullname" . }}-controller-manager
      terminationGracePeriodSeconds: 10
